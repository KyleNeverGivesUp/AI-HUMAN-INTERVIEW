# 🎯 简化版数字人测试指南

## ✅ 已完成修改

我已经将系统改成**简化版**，不需要任何 API Keys 就可以测试！

---

## 🔧 简化版特点

### ❌ 不需要：
- ❌ LiveKit API Key
- ❌ Tavus API Key  
- ❌ 任何外部服务配置
- ❌ Docker 部署

### ✅ 只需要：
- ✅ 后端服务器运行（已在运行中）
- ✅ 前端服务器运行（已在运行中）
- ✅ 浏览器

---

## 🎮 测试步骤

### 第1步：访问数字人页面

在浏览器访问：
```
http://localhost:3000/digital-human
```

或者点击 JobNova 左侧菜单的 **"AI Mock Interview"**

---

### 第2步：点击 "Start Session"

你会看到：
- ✅ 连接成功
- ✅ AI 发送欢迎消息："你好！我是简化版 AI 助手。请输入 'hello' 试试看！"

---

### 第3步：测试对话

#### ✅ 测试 1：输入 "hello"（正确）

**输入：**
```
hello
```

**AI 回复：**
```
今天天气很好，有什么可以帮你的
```

---

#### ❌ 测试 2：输入其他内容（会提示）

**输入：**
```
你好
```

**AI 回复：**
```
抱歉，我现在只能回复 'hello'。请输入 'hello' 试试！
```

---

**输入：**
```
How are you?
```

**AI 回复：**
```
抱歉，我现在只能回复 'hello'。请输入 'hello' 试试！
```

---

## 🎯 测试界面说明

### 界面元素：

```
┌──────────────────────────────────────────┐
│     AI Digital Human Demo                │
│                                          │
│  [视频区域 - 显示提示信息]                │
│   "AI 已就绪，等待你的消息"               │
│   "💡 提示：输入 'hello' 获得回复"        │
│                                          │
│  ┌───────────────────┐                  │
│  │ Conversation      │                  │
│  │                   │                  │
│  │ AI: 你好！我是简化版│                  │
│  │     AI 助手...     │                  │
│  │                   │                  │
│  │ You: hello        │                  │
│  │                   │                  │
│  │ AI: 今天天气很好...│                  │
│  │                   │                  │
│  │ [输入框...]       │                  │
│  └───────────────────┘                  │
└──────────────────────────────────────────┘
```

---

## 🔍 后端逻辑说明

### 简化的处理流程：

```python
def process_message(message):
    if message.lower().strip() == "hello":
        return "今天天气很好，有什么可以帮你的"
    else:
        return "抱歉，我现在只能回复 'hello'。请输入 'hello' 试试！"
```

### 代码位置：
```
backend/src/services/digital_human_service.py
- process_message() 方法
```

---

## 📝 测试清单

### 基础功能：
- [ ] 页面能正常打开
- [ ] 点击 "Start Session" 成功
- [ ] 看到 AI 欢迎消息
- [ ] 输入 "hello" 收到正确回复
- [ ] 输入其他内容收到提示消息
- [ ] 消息历史正确显示
- [ ] 可以多次对话

### 交互功能：
- [ ] 发送按钮可点击
- [ ] 回车键可发送
- [ ] 处理中显示加载动画
- [ ] 结束会话按钮工作
- [ ] 可以重新开始会话

### 界面功能：
- [ ] 聊天记录自动滚动
- [ ] 用户消息靠右显示（紫色）
- [ ] AI 消息靠左显示（灰色）
- [ ] 提示信息清晰可见

---

## 🎨 界面提示

### 启动前：
```
"这是简化版演示。输入 'hello' 试试看！"
```

### 连接后：
```
"AI 已就绪，等待你的消息"
"💡 提示：输入 'hello' 获得回复"
"(简化版：不需要 API keys)"
```

### 聊天窗口：
```
"💬 开始对话"
"提示：输入 'hello' 获得回复"
```

---

## 🐛 常见问题排查

### 问题 1：点击 Start Session 没反应

**检查：**
```bash
# 后端是否运行？
curl http://localhost:8000/api/health

# 应该返回：
{"status":"healthy","version":"0.1.0",...}
```

---

### 问题 2：发送消息没有回复

**检查浏览器控制台：**
- 按 F12 打开开发者工具
- 查看 Console 标签是否有错误
- 查看 Network 标签查看 WebSocket 连接

---

### 问题 3：WebSocket 连接失败

**检查后端日志：**
```bash
# 后端终端应该显示：
INFO: WebSocket connected for room: room-xxxxx
```

---

## 🔄 重启服务

### 如果遇到问题，重启服务器：

**前端：**
```bash
cd /Users/kyle/Projects/AI-HUMAN-INTERVIEW/frontend
# Ctrl+C 停止
npm run dev
```

**后端：**
```bash
cd /Users/kyle/Projects/AI-HUMAN-INTERVIEW/backend
source .venv/bin/activate
# Ctrl+C 停止
python -m uvicorn src.main:app --reload
```

---

## 🎯 测试场景

### 场景 1：正常对话流程
```
1. 打开页面
2. Start Session
3. 输入 "hello"
4. 收到回复 ✅
5. 再输入 "hello"
6. 再次收到回复 ✅
```

### 场景 2：错误输入处理
```
1. 输入 "hi"
2. 收到提示：只支持 hello ✅
3. 输入 "hello"
4. 正常回复 ✅
```

### 场景 3：大小写测试
```
1. 输入 "HELLO"
2. 应该收到回复 ✅（忽略大小写）
3. 输入 "Hello"
4. 应该收到回复 ✅
```

---

## 📊 预期效果

### 成功的对话示例：

```
AI: 你好！我是简化版 AI 助手。请输入 'hello' 试试看！

You: hello

AI: 今天天气很好，有什么可以帮你的

You: hello

AI: 今天天气很好，有什么可以帮你的

You: how are you

AI: 抱歉，我现在只能回复 'hello'。请输入 'hello' 试试！

You: hello

AI: 今天天气很好，有什么可以帮你的
```

---

## 💡 后续扩展

### 如果想添加更多规则：

编辑 `backend/src/services/digital_human_service.py`：

```python
async def process_message(self, room_name: str, message: str) -> dict:
    user_message = message.strip().lower()
    
    # 添加更多规则
    if user_message == "hello":
        response_text = "今天天气很好，有什么可以帮你的"
    elif user_message == "bye":
        response_text = "再见！祝你有美好的一天！"
    elif user_message == "天气":
        response_text = "今天是晴天，适合出门！"
    else:
        response_text = "抱歉，我现在只能回复特定关键词。"
    
    return {"response": response_text, ...}
```

---

## ✅ 总结

这个简化版让你可以：
1. ✅ **测试完整的前后端流程**
2. ✅ **不需要任何外部 API**
3. ✅ **快速验证功能**
4. ✅ **为将来集成真实 API 做准备**

**现在就去测试吧！** 🚀

访问：http://localhost:3000/digital-human
